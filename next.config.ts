import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com','assets.aceternity.com','plus.unsplash.com','s3.amazonaws.com',],
  },
};

export default nextConfig;
